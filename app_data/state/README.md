# app_data/state

This directory is for local runtime cache and state created by app-facing workflows.

Keep live outputs local. Public commits should only add templates or sanitized fixtures when they are genuinely needed.
