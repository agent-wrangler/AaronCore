# 当前主链

更新时间：2026-03-16 02:42

## 当前唯一运行路径

```text
desktop.py
  -> agent_final.py (FastAPI, port 8090)
  -> output.html (78 行 HTML 骨架)
     ├── static/css/main.css (全部样式)
     └── static/js/ (7 个 JS 模块)
         utils.js → awareness.js → chat.js → memory.js → settings.js → docs.js → app.js
```

## 当前事实

- 桌面壳入口是 `desktop.py`
- 后端唯一主入口是 `agent_final.py`（993 行 + 21 个 core 模块）
- 前端骨架是 `output.html`（78 行），CSS/JS 在 `static/` 目录
- 静态资源由 FastAPI StaticFiles 挂载 `/static` 路径提供
- 当前技能目录是 `core/skills/`
- 当前统一状态目录是 `memory_db/`

## 不是当前主链的文件

- `archive/legacy-entrypoints/agent.py`
- `archive/legacy-entrypoints/agent_simple.py`
- `archive/legacy-entrypoints/agent_fixed.py`
- `archive/legacy-entrypoints/agent_patch.py`
- `archive/legacy-ui/output_optimized.html`
- 所有 `*.bak*`、`*.backup*`

## 当前对外接口

- `GET /` — 返回 output.html
- `GET /static/*` — 静态资源（CSS/JS）
- `POST /chat`
- `GET /stats`
- `POST /stats`
- `GET /nova_name`
- `GET /skills`
- `GET /history`
- `GET /memory`
- `GET /health`
- `GET /awareness/pending`
- `GET /autolearn/config`
- `POST /autolearn/config`
- `GET /autolearn/diagnose`
- `GET /self_repair/status`
- `GET /self_repair/reports`
- `POST /self_repair/propose`
- `POST /self_repair/preview`
- `POST /self_repair/apply`
- `GET /docs/index`
- `GET /docs/content`
