# 后续收口路线

最后更新：2026-03-16 02:42

## 第一阶段 — 已完成

- 保持 `agent_final.py` 作为唯一运行入口
- 保持 `memory_db/` 作为唯一主状态目录
- 保持 `output.html` 作为唯一主页面
- 5 个技能接入，L1-L8 八层记忆跑通，51 个测试通过

## 第二阶段 — 后端约 70% 完成

- `agent_final.py` 已从 1874 行拆到 993 行，提取出 21 个 core 模块
- 新增 `l2_memory.py`（547 行）作为 L2 持久记忆中枢
- 所有模块使用 `init()` 依赖注入，导入方向单向无循环
- 尚未拆到 `api/`、`services/`、`storage/` 粒度

## 第三阶段 — 已完成

- `output.html` 从 3003 行拆到 78 行纯 HTML 骨架
- CSS/JS 按职责拆分到 `static/` 目录（1 个 CSS + 7 个 JS 模块）
- `agent_final.py` 挂载 FastAPI StaticFiles 服务静态资源
- 跨模块共享函数统一到 `utils.js`

## 第四阶段 — 待定

- 清理仓库里剩余的历史噪音文件
- 视情况补 `requirements.txt` 或 `pyproject.toml`
- 再决定是否继续做更完整的模块化重构（`api/`、`services/`、`storage/`）

## 第五阶段（未来预研）

- 在单机主链稳定后，再评估 `Agent ID` 与 `Agent -> Agent` 协作方向
- 预研每个客户端拥有独立 `Agent ID` 的身份模型
- 预研 Agent 间消息、关系、协作与安全边界
- 把”多 Agent 社交与集体进化”保持在中远期探索，不提前混入当前主实现
