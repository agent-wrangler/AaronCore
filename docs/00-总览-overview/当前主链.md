# 当前主链

更新时间：2026-03-13

## 当前唯一运行路径

```text
desktop.py
  -> agent_final.py
  -> http://localhost:8090/
  -> output.html
```

## 当前事实

- 桌面壳入口是 `desktop.py`
- 后端唯一主入口是 `agent_final.py`
- 前端唯一主页面是 `output.html`
- 当前技能目录是 `core/skills/`
- 当前统一状态目录是 `memory_db/`

## 不是当前主链的文件

- `archive/legacy-entrypoints/agent.py`
- `archive/legacy-entrypoints/agent_simple.py`
- `archive/legacy-entrypoints/agent_fixed.py`
- `archive/legacy-entrypoints/agent_patch.py`
- `archive/legacy-ui/output_optimized.html`
- 所有 `*.bak*`、`*.backup*`

## 当前对外接口

- `POST /chat`
- `GET /stats`
- `POST /stats`
- `GET /nova_name`
- `GET /skills`
- `GET /history`
- `GET /memory`
- `GET /health`
